---
# https://www.gitpod.io/blog/gitpodify/
github:
  prebuilds:
    branches: true
    pullRequestsFromForks: true
    addBadge: true
    addComment: true
    addLabel: true

# https://www.gitpod.io/blog/docker-in-gitpod/
image:
  file: .gitpod.Dockerfile
#  does not not seem to work need to run as root at the end
# image: restartus/src
